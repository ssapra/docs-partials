* **Authorization for Okta OIDC:** When using an Okta OIDC provider, the roles claim in the ID token does not get populated with external IdP groups. This impacts mapping of external IdP groups to scopes. Despite this limitation, you are still able to use Okta OIDC provider for authentication.