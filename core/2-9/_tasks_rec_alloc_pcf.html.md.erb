You configure the default memory and disk allocations using the **Default app memory** and **Default disk quota per app** fields in the **App Developer Controls** pane of the Pivotal Application Service (PAS) tile.
