To view usage statistics for any service, do the following:

1. Run the following command:

	```
	bosh -d DEPLOYMENT-NAME vms --vitals
	```

1. To view process-level information, run:

	```
	bosh -d DEPLOYMENT-NAME instances --ps
	```
