* **Multi-Site Replication is incompatible with <%= vars.app_runtime_abbr %> v2.10:**
      If any service instances is using the Multi-Site Replication plan, the service instance does not update successfully.
      This impacts the `upgrade-all-service-instances` errand as well.
