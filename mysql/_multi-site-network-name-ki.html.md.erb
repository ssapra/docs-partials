* **Uppercase letters in service network name blocks Multi-Site Replication setup**
      BOSH DNS can only resolve lowercase letters. Since the service network name is only used to
      reference the appropriate network configuration in BOSH, the network name could be changed to use lowercase letters.
